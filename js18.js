document.querySelectorAll('.read-more').forEach(button => {
  button.addEventListener('click', () => {
    alert('Redirecting to the full post...');
    // Here, you can add functionality to redirect to the full post page
  });
});
